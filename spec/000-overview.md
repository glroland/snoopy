# Overview

> Status: draft
> This is the one spec file that isn't a single feature — it's the project-level
> context every other spec file assumes. Keep it short. Fill it in with `/spec-new`
> or by hand whenever you're ready.

## What this app is

<One paragraph: what it does, who it's for (you), why it exists.>

## Constraints

<Tech stack preferences, platforms it must run on, things that are off the table
(e.g. "no cloud dependency", "must run on a Raspberry Pi"), anything non-negotiable.>

## Domains

<List of the feature/domain areas this app is broken into, each with its own
spec file in this directory. Update this list as you add spec files with
`/spec-new`.>

- (none yet)
